resources:
- name: concourse-examples
  type: git
  icon: github
  source:
    uri: https://github.com/zak-02/submodule/tree/b2c81dc79a6c90aa417e7c50990ad30288e18ada

jobs:
- name: execute-code
  plan:
  - get: netflix
  - task: cat-netflix
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: busybox
      inputs: # pass concourse-examples into this task step
      - name: concourse-examples
      run:
        path: cat
        args: ["netflix.py']