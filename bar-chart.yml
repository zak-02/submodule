resources:
- name: concourse-examples
  type: git
  icon: github
  source:
    uri: https://github.com/zak-02/submodule/tree/3262abede41779ade2d050da6f0d5d3524af3f6f

jobs:
- name: execute-code
  plan:
  - get: netflix
  - task: cat-netflix
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: busybox
      inputs: # pass concourse-examples into this task step
      - name: concourse-examples
      run:
        path: cat
        args: ["netflix.py"]