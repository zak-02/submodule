resources:
- name: concourse-examples
  type: git
  icon: github
  source:
    uri: https://github.com/zak-02/Projects

jobs:
- name: run-job
  plan:
  - get: concourse-examples
  - task: run-task
config:
  platform: linux
  image_resource:
    type: registry-image
  source:
    repository: python
run:
  path: python  # Use Python to run a script or command
  args: ["consoure-examples/submodule/netflix.py"]
